{
  "name": "npm-test",
  "version": "1.0.0",
  "description": "A lightweight task scheduler that allows developers to run specific tasks at predetermined times or intervals, similar to cron jobs but simpler.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
